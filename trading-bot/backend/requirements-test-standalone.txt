# Standalone test dependencies — does not inherit requirements.txt
# Installs only what the unit + integration tests need.
# Production deps with C-extensions (pandas-ta, kiteconnect, backtrader, etc.)
# are NOT required by the test suite — they are mocked or not exercised.

# Core test framework
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0

# Async HTTP client (integration tests)
httpx==0.27.0
anyio>=4.0

# FastAPI + ASGI (integration tests spin up the app)
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
pydantic-settings==2.3.1

# Database (integration tests use SQLite in-memory)
sqlalchemy[asyncio]==2.0.30
aiosqlite==0.20.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
pyotp==2.9.0
cryptography==42.0.8

# Math / engines
scipy>=1.11
numpy>=1.24

# Redis (mocked in unit tests; real client imported for type hints)
redis==5.0.4

# Celery (needed for imports in strategy_worker)
celery==5.4.0

# HTTP (delta adapter)
httpx==0.27.0

# Misc
python-dotenv==1.0.1
